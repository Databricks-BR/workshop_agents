# Databricks Configuration - Choose ONE authentication method
DATABRICKS_HOST="https://e2-demo-field-eng.cloud.databricks.com"

# Option 1: Personal Access Token (simpler for development)
DATABRICKS_TOKEN=your-databricks-personal-access-token

# Option 2: Service Principal OAuth (recommended for production)
# Uncomment these and comment out DATABRICKS_TOKEN if using OAuth
#DATABRICKS_CLIENT_ID="your-service-principal-client-id"
#DATABRICKS_CLIENT_SECRET="your-service-principal-secret"

# WARNING: Do NOT set both TOKEN and CLIENT_ID/CLIENT_SECRET - this will cause auth conflicts!

# LLM Model Configuration
LLM_ENDPOINT_NAME=databricks-gpt-oss-20b
SYSTEM_PROMPT="You are a helpful assistant. Use the available tools to answer questions."

# Tools Configuration (Optional)
# UC Tools - Comma-separated list of Unity Catalog function names
# Example: UC_TOOL_NAMES="catalog.schema.function1,catalog.schema.function2"
#UC_TOOL_NAMES=""

# Vector Search Tools - Comma-separated list of vector search index names  
# Example: VECTOR_SEARCH_INDEX_NAMES="index1,index2,index3"
#VECTOR_SEARCH_INDEX_NAMES=""

# Note: No Lakebase/Database configuration needed for simple version!
